{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"CROID","text":"<p>The CROID (Crossref Research Object ID) system provides unique, URL-safe identifiers for research objects to track metadata across different systems.</p>"},{"location":"#overview","title":"Overview","text":"<p>A CROID acts as an identifier separate from others like DOIs or ORCIDs, allowing tracking of research objects regardless of metadata changes or missing standards.</p>"},{"location":"#api-reference","title":"API Reference","text":""},{"location":"#create-croid","title":"Create CROID","text":"<p>POST <code>/croid</code></p> <p>Links an object's identity (via <code>cro_type</code>, <code>cro_value</code>, <code>system</code>) to a CROID.</p> <p>Request Body:</p> <pre><code>{\n  \"cro_type\": \"DOI\",\n  \"cro_value\": \"10.5555/12345678\",\n  \"system\": \"nauvis\"\n}\n</code></pre> <p>Responses: - <code>201 Created</code>: New CROID minted. - <code>200 OK</code>: Existing CROID found (Idempotent). - <code>400 Bad Request</code>: Missing or invalid parameters.</p>"},{"location":"#retrieve-metadata","title":"Retrieve Metadata","text":"<p>GET <code>/croid/{croid}</code></p> <p>Fetches the metadata associated with a specific CROID.</p> <p>Response Body:</p> <pre><code>{\n  \"cro_type\": \"DOI\",\n  \"cro_value\": \"10.5555/12345678\",\n  \"system\": \"nauvis\",\n  \"croid\": \"aaaaa12345_aaaaa12345-aaaaa12345\",\n  \"created_at\": \"2026-05-28T19:04:17Z\"\n}\n</code></pre> <p>Responses: - <code>200 OK</code>: Success. - <code>404 Not Found</code>: CROID does not exist.</p>"},{"location":"#implementation-details","title":"Implementation Details","text":"<ul> <li>Language: Go 1.25</li> <li>Database: SQLite (using <code>modernc.org/sqlite</code>: a pure-Go, CGO-free driver).</li> <li>Querying: <code>sqlc</code> generates type-safe database access logic.</li> <li>Concurrency: The service is idempotent; concurrent creation requests for the same identity safely return the single existing CROID.</li> </ul>"},{"location":"#client-usage","title":"Client Usage","text":"<p>Other systems mint CROIDs by importing the shared HTTP client package:</p> <pre><code>import \"github.com/nexus/croid/client\"\n\nc := client.New(\"http://localhost:8080\")\nminted, err := c.Mint(ctx, client.Identity{\n    Type:     \"DOI\",\n    Value:    \"10.1/paper\",\n    System:   \"nauvis\",\n    Record:   []byte(`{\"title\":\"...\",\"DOI\":\"10.1/paper\"}`),\n})\n</code></pre> <p>The client is used by both Nauvis and Fulgora, providing a uniform interface for minting CROIDs and publishing them to RabbitMQ.</p>"},{"location":"#quick-start","title":"Quick Start","text":"<p>To run the service:</p> <pre><code>go run main.go --addr :8080 --db croid.sqlite3\n</code></pre> <p>To test via HTTP:</p> <pre><code>curl -X POST -H \"Content-Type: application/json\" -d '{\"cro_type\":\"DOI\",\"cro_value\":\"10.5555/example\",\"system\":\"nauvis\"}' http://localhost:8080/croid\n</code></pre>"},{"location":"#cli-generate-a-croid-from-the-command-line","title":"CLI: Generate a CROID from the command line","text":"<p>The CLI can mint a CROID without starting the HTTP server, using the <code>--generate</code> flag. It runs the exact same code path as <code>POST /croid</code> (same SQLite setup, schema, and <code>store.Create</code>), so it mints, dedupes, and validates identically:</p> <pre><code>go run . --generate --db croid.sqlite3\n</code></pre> <p>With a JSON identity input, the CLI resolves or mints a CROID for that identity (deduplicating against whatever already lives in <code>--db</code>):</p> <pre><code>go run . --generate --db croid.sqlite3 --input '{\"cro_type\":\"DOI\",\"cro_value\":\"10.5555/example\",\"system\":\"nauvis\"}'\n</code></pre> <p>The output matches the <code>POST /croid</code> response body:</p> <pre><code>{\n  \"cro_type\": \"DOI\",\n  \"cro_value\": \"10.5555/example\",\n  \"system\": \"nauvis\",\n  \"croid\": \"aaaaa12345_aaaaa12345-aaaaa12345\",\n  \"created_at\": \"2026-09-17T20:11:10Z\"\n}\n</code></pre> <p>Re-running the command for the same identity returns the same CROID (<code>\"created_at\"</code> unchanged) rather than minting a new one. If <code>--input</code> omits a required field (<code>cro_type</code>, <code>cro_value</code>, or <code>system</code>) the command exits non-zero with an error, mirroring the <code>400 Bad Request</code> the HTTP endpoint returns.</p>"}]}
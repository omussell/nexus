# Pomus

A frontend app for nicely displaying the data output from Aquilo and Nexus.

Minimal and server side rendered. HTML first. Uses Pico CSS for simple styling. Uses HTMX for handling API requests.

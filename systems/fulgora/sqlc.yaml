version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/migrate/schema.sql"
    queries: "db/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_interface: true

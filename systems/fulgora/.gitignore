# The ignore patterns that used to live here have moved up to the repo root
# .gitignore at https://github.com/nexus/nexus
# The data-artifact patterns below are retained for reference and local use
# only; if they ever stop matching after a repo move, re-add them to the root.

*/initial_input/
ror/output/
retractionwatch/output/

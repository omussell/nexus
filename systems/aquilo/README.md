# AQUILO

- Combine all the data we've collected into the usable JSON output

Once all of the data processing is complete and the metadata quality has been improved, we need an API to provide the data in JSON format so that other organizations can access it.
